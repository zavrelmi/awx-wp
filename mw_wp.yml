 - hosts: 10.0.125.73
   gather_facts: true
   vars:
     deploy_host: 10.0.125.72
   
   #vars_prompt:
    #- name: deploy_host
     # prompt: direcion de host para hacer la subida de base de datos?
      #private: no 
      
   tasks:
     - name: Transfer file from ServerA to ServerB
       synchronize:
         src: /home/ansible/ #/var/www/html/
         dest: /home/ansible/
         mode: pull
         owner: yes
         group: yes
         recursive: yes
       delegate_to: "{{ deploy_host }}"
