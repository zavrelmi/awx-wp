 - hosts: 10.0.125.73
   gather_facts: true
   vars:
     deploy_host: 10.0.125.72
   
   #vars_prompt:
    #- name: deploy_host
     # prompt: direcion de host para hacer la subida de base de datos?
      #private: no 
      
   tasks:
   - name: Copy wordpress to remote host
   become: true
    copy:
     src: "/home/ansible/latest.tar.gz"
     dest: /tmp/migrate_wp.tar.gz
     owner: root
     group: root
     mode: 0600
